---
- name: Service ready message template generator
  hosts: localhost
  vars_files:
    - vars.yml
  vars:
    curlytag: "{{ '{{' |string }}"
  tasks:
    - name: Create service ready message html
      template:
          src: template.html.j2
          dest: service-ready-message-template.html

    - name: reset as variables
      replace:
        path: service-ready-message-template.html
        regexp: '--curly'
        replace: "{{ '{{' |string }}"

    - name: reset as variables
      replace:
        path: service-ready-message-template.html
        regexp: 'curly--'
        replace: "{{ '}}' |string }}"